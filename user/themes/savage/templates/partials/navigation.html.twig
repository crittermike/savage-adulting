<nav class="navbar navbar-inverse navbar-static-top">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle navbar__toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a href="{{ home_url }}">
            <img class="navbar__logo" src="{{ base_url_relative }}{{ site.logo }}" alt="Blog Logo" />
        </a>
    </div>
    <div id="navbar" class="navbar-collapse collapse" aria-expanded="false" style="height: 1px;">
        <ul class="nav navbar-nav">
        </ul>
        <ul class="nav navbar-nav navbar-right navbar__links">
            {% for mitem in site.links %}
                <li>
                    <a class="navbar__link" href="{{ mitem.url }}">
                        {{ mitem.title }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div><!--/.nav-collapse -->
</nav>